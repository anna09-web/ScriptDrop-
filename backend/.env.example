# Anthropic — the ONLY place this key may ever live
ANTHROPIC_API_KEY=

# Supabase — service role key is server-only, never expose to the browser
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App
FRONTEND_URL=http://localhost:5173
PORT=3001
